<h2>Edit Post</h2>

<form class="post-edit-form">
    <div>
        <input id="post-title" type="text" value="{{post.title}}"/>
    </div>
    <div>
        <textarea id="post-content">{{post.post_content}}</textarea>
    </div>
    <div>
        Posted by {{post.username}}
    </div>
</form>